pi/
db/
# docs/ stays private, except the rollout plan (the source of truth for hosting).
# Must be docs/* not docs/ — git can't re-include a file inside an ignored directory.
docs/*
!docs/rollout-plan.md
reference/
reverse-engineering/
dtb-backup.img
*.png
*.jpg
*.img
README.md
# Keep the board graphic — used in the app UI
!ProjectBoard.png
# PWA icons (generated by make_icons.py) and manifest screenshots
!icon-*.png
!apple-touch-icon.png
!screenshot-*.png

# Build output for Cloudflare Workers (regenerated by build.sh)
public/
